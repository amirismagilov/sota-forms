name: Deploy to production

# Выкат на 45.131.40.65 (nginx + https://sota-forms.im-iaa.ru).
#
# Схема на хосте нестандартная, и деплой обязан ей соответствовать:
# фронт там НЕ сервис, а разовая build-job — nginx отдаёт статику из
# frontend/dist, а `docker compose up` фронт пропускает (profiles: ["build"]
# в server-local docker-compose.override.yml). Поэтому одного `up --build`
# мало: без ./rebuild-frontend.sh деплой выкатывает новый бэкенд со старым
# интерфейсом — молча и с зелёной галочкой.

on:
  push:
    branches: [main]
  # Чтобы перевыкатить без пустого коммита (например, после правки секретов).
  workflow_dispatch:

jobs:
  deploy:
    name: Deploy sota-forms
    runs-on: ubuntu-latest
    steps:
      - name: Проверить, что секреты заданы
        id: secrets
        env:
          SSH_HOST: ${{ secrets.SSH_HOST }}
          SSH_USER: ${{ secrets.SSH_USER }}
          SSH_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
        run: |
          # Незаданные секреты — это НЕ поломка, а «выкатываем руками».
          # Раньше job падал с «error: missing server host», и каждый коммит в main
          # получал красный крест, по которому не отличить настоящую аварию от
          # осознанной ручной схемы. Теперь — предупреждение и пропуск.
          missing=""
          [ -n "$SSH_HOST" ] || missing="$missing SSH_HOST"
          [ -n "$SSH_USER" ] || missing="$missing SSH_USER"
          [ -n "$SSH_KEY" ]  || missing="$missing SSH_PRIVATE_KEY"
          if [ -n "$missing" ]; then
            echo "::warning::Автодеплой пропущен — не заданы секреты:$missing (Settings → Secrets and variables → Actions). Выкат руками: ./scripts/deploy.sh"
            echo "configured=false" >> "$GITHUB_OUTPUT"
          else
            echo "configured=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Deploy via SSH
        if: steps.secrets.outputs.configured == 'true'
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          passphrase: ${{ secrets.SSH_PASSPHRASE }}
          # Сборка фронта на этой VPS занимает под минуту, бэкенда — больше;
          # дефолтных 10m хватает впритык, а обрыв на середине сборки хуже ожидания.
          command_timeout: 20m
          script: |
            set -euo pipefail
            cd /opt/sota-forms

            # Код тянем здесь, а не внутри скрипта: самого scripts/deploy.sh на
            # хосте может ещё не быть — он приезжает этим же pull'ом.
            echo "==> обновляем код"
            git pull origin main

            # Вся последовательность выката живёт в одном месте — в репозитории.
            # Так ручной выкат и автодеплой не могут разъехаться: и человек, и CI
            # запускают буквально один и тот же скрипт.
            ./scripts/deploy.sh --skip-pull
